using Caisse1.Data;
using Caisse1.Models;
using Microsoft.EntityFrameworkCore;

namespace Caisse1.Services
{
    public class CaisseServices
    {
      /*  private readonly CaisseContext _context;

        public CaisseServices(CaisseContext context)
        {
            _context = context;
        }

        public IEnumerable<Produit> GetAll()
        {
            return _context.Produits
                .AsNoTracking()
                .ToList();
        }*/
    }
}
